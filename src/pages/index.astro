---
import Base from "../layouts/Base.astro";
import Nav from "../components/Nav";
import Footer from "../components/Footer";
import Hero from "../components/home/Hero";
import DocketShowcase from "../components/home/DocketShowcase";
import Problem from "../components/home/Problem";
import Architecture from "../components/home/Architecture";
import Products from "../components/home/Products";
import WhyUs from "../components/home/WhyUs";
import UseCases from "../components/home/UseCases";
import SocialProof from "../components/home/SocialProof";
import BottomCTA from "../components/home/BottomCTA";

const siteOrigin = (Astro.site?.toString() || "https://karanggip.github.io").replace(/\/$/, "");
const base = import.meta.env.BASE_URL.replace(/\/$/, "");

const productCatalogLd = {
  "@context": "https://schema.org",
  "@type": "ItemList",
  "name": "Guarded Growth IP Products",
  "itemListElement": [
    { "@type": "ListItem", "position": 1, "name": "DocketEngine",  "url": `${siteOrigin}${base}/products/docketengine/` },
    { "@type": "ListItem", "position": 2, "name": "RenewalEngine", "url": `${siteOrigin}${base}/products/renewalengine/` },
  ],
};
---

<Base
  title="Guarded Growth · The Data Infrastructure for Global IP"
  description="Modern IP management platform with automated docketing and renewals across 100+ jurisdictions. Purpose-built engines on a cloud-native data infrastructure."
  jsonLd={productCatalogLd}
>
  <Nav client:load />
  <main>
    <Hero client:load />
    <DocketShowcase client:visible />
    <Problem client:visible />
    <Architecture client:visible />
    <Products client:visible />
    <WhyUs client:visible />
    <UseCases client:visible />
    <SocialProof client:visible />
    <BottomCTA client:visible />
  </main>
  <Footer />
</Base>
